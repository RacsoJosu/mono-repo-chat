import { PantallaContenido } from "@/componentes/layout/pantalla-contenido";

export function PantallaContactos() {
  return (
    <PantallaContenido
      titulo="Contactos"
      descripcion="Gestiona las personas que escriben por WhatsApp."
    />
  );
}
