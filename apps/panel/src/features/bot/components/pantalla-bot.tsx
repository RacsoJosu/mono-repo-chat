import { PantallaContenido } from "@/componentes/layout/pantalla-contenido";

export function PantallaBot() {
  return (
    <PantallaContenido titulo="Bot" descripcion="Configura los flujos y respuestas del chatbot." />
  );
}
